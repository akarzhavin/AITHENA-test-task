services:
  analyzer:
    build:
      context: .
      dockerfile: Dockerfile
    # Connect the .env file, which should contain OPENAI_API_KEY
    env_file:
      - .env
    # Mount the entire project for development and testing,
    # as well as the data/ and output/ directories for data
    volumes:
      - .:/app